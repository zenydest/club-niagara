# Necesario para que React Native pueda encontrar @react-native/gradle-plugin
# durante el build de Android. pnpm en modo isolated (default) no crea symlinks
# para dependencias transitivas, pero el build de Gradle las necesita en una
# estructura flat. node-linker=hoisted crea un node_modules plano solo para
# este workspace.
node-linker=hoisted
